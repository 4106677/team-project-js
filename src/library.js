// смена фона через джиес+подключить(Лучше через CSS все же)
// отцентровать кнопки,фон изображение и границы контенера

// повесить слушателя на input
// функция которая будет запрашивать отдельно каждую кнопку
const checkBntLibrary = document.querySelector('.storage__input');
const bgrImgLibrary = document.querySelector('header');
// bgrImgLibrary.style.backgroundImage = 'url(../images/header-bcg.jpg)';
